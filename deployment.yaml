apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginx
  namespace: {{ ns }}
  labels:
    name: nginx
    projectkey: {{ projectkey }}
    AppID: {{ AppID }}
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
