apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: {{ ns }}
  labels:
    name: nginx
    AppID: {{ AppID }}
    ProjectKey: {{ PK }}
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
